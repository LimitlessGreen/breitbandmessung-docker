#!/bin/bash

# Load container environment
[ -f /etc/container_environment.sh ] && source /etc/container_environment.sh

# Start the Master Automation Python script
# This script handles the BNetzA campaign logic and UI automation via AT-SPI
echo "[MasterAutomation] Starting Python automation service..."
exec python3 /usr/local/bin/master-automation.py
